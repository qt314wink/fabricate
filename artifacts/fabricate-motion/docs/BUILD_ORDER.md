# Build order — work this environment cannot finish

**Author:** Jennipher Troup (`melodicbloom`)  
**Date:** 2026-09-14

These items need a real machine, a browser, npm, a mill, or a vendor.
They are **additions**, not unpaid repairs on CORE.

Prompts to hand another agent: `docs/BUILD_PROMPTS.md`.

## Sequence

| # | ID | Owner machine | Why not here | Done when |
|---|---|---|---|---|
| 1 | LOCK | laptop with npm | No durable lockfile in this sandbox | `npm ci && npm test` green |
| 2 | PROOF | same + capture | Cannot shoot a 6s split from Vite here | Film matches `PROOF_FILM.md`; 30 and 60 end pose agree |
| 3 | QUEUE (code in-repo; verify in profiler) | browser profiler | Worker returns STL text, not mesh buffers | Dirty-key rebuild never in `useFrame` |
| 4 | HOVER (code in-repo; verify &lt;16ms on device) | device lab | No GPU pointer budget measured | Pointermove &lt;16 ms, no React setState |
| 5 | POST (G2 holes in-repo; vendor dialect + mill still yours) | shop + one controller | No GRBL box attached | One machine cuts a plate from `bracket.nc` *after* a human diffs the file |
| 6 | STOCK | GPU + fixtures | Voxel peel is a product | Chip-away matches posted Z |
| 7 | CART | Shopify partner | No storefront credentials | Line-item properties survive checkout |
| 8 | KERNEL | OCCT/Manifold license | WASM kernel is a company | Manifold check + volume vs estimate |
| 9 | MANDREL | jeweler bench | Torus ≠ comfort-fit | Size within 0.2 mm of a real mandrel |
| 10 | PUBLISH | GitHub `melodicbloom` | Org push is yours | Tagged release + LICENSE intact |

Do **not** reorder 5 before 1–3. A pretty NC file that hitch-stalls the canvas is how this class of tool gets returned.

## Spoken limits that stay even after #5

Preview post ≠ cutter comp ≠ stock sim ≠ vendor dialect. Those are SKUs 5–6, sold separately.
